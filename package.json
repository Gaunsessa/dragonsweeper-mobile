{
  "name": "dragonsweeper-mobile",
  "version": "1.1.18",
  "description": "Dragonsweeper - A roguelike minesweeper adventure mobile app",
  "main": "index.js",
  "scripts": {
    "build": "echo 'No build step required - static HTML5 game'",
    "sync": "npx cap sync",
    "android": "npx cap open android",
    "android:build": "cd android && ./gradlew assembleRelease",
    "android:build:debug": "cd android && ./gradlew assembleDebug"
  },
  "keywords": [
    "dragonsweeper",
    "minesweeper",
    "roguelike",
    "game",
    "mobile"
  ],
  "author": "Daniel Benmergui",
  "license": "MIT",
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
